EnergyHealing Node/Express
==

Node + Gulp + Ejs + Less + js + LiveReload + Bower

# Install

1\. Run **npm** command to install the dependencies.

    npm install
    bower install

2\. Run Gulp.

    node start

It's work!
